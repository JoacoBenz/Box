# next-auth@5 beta declara peerOptional nodemailer@^7, pero usamos nodemailer@9
# (fix de vulnerabilidades SMTP). next-auth solo usa nodemailer para su email
# provider, que no usamos (credentials + Google). Sacar cuando next-auth acepte v9.
legacy-peer-deps=true
